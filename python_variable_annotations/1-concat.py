#!/usr/bin/env python3
"""Module with function that that concatenates 2 strings"""


def concat(str1: str, str2: str) -> str:
    """Function that takes a string str1 and a string str2 as arguments
    and returns a concatenated string"""
    return str1 + str2
